# Legacy PCLA base-image dependency inventory.
#
# Do not install this file into the wrapper environment. The pinned PCLA Docker
# base supplies model dependencies, while pyproject.toml and uv.lock manage the
# top-level wrapper dependencies.
